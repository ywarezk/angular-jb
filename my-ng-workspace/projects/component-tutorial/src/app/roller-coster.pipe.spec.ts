import { RollerCosterPipe } from './roller-coster.pipe';

describe('RollerCosterPipe', () => {
  it('create an instance', () => {
    const pipe = new RollerCosterPipe();
    expect(pipe).toBeTruthy();
  });
});
